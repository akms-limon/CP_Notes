#include <bits/stdc++.h>
#define ll long long
using namespace std;

int32_t main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    int t; cin >> t;
    cout << "limon";
    // while (t--) {
    // 	int n; cin >> n;
    // 	vector<int> bas, panda;
    // 	for (int i = 0; i < n; i++) {
    // 		int x; cin >> x;
    // 		bas.push_back(x);
    // 	}
    // 	for (int i = 0; i < n; i++) {
    // 		int x; cin >> x;
    // 		panda.push_back(x);
    // 	}
    // 	for (auto it : bas) {
    // 		cout << it << '\n';
    // 	}
    // 	// sort(bas.begin(), bas.end());
    // 	// sort(panda.begin(), panda.end());
    // 	// ll ans = 1;
    // 	// for (int i = 0; i < n; i++) {
    // 	// 	ll cnt = upper_bound(bas.begin(), bas.end(), panda[i]) - bas.begin();
    // 	// 	if (cnt - i <= 0) ans = 0;
    // 	// 	ans *= (cnt - i);
    // 	// }
    // 	// cout << ans << '\n';
    // }
    return 0;
}